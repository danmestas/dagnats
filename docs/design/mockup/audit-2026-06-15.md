# Console vs Mockup — Systematic Audit (2026-06-15)

Live: `:5390` (freshly seeded). Mockup: `/tmp/dagnats-mockups/*.tsx`. Auditors: 5 (Observe/Inventory/Operate/System/Trace), one per mockup view file.

Telemetry graph-density (sparse throughput/p50/sparklines/charts) excluded — already diagnosed as a separate telemetry-pipeline issue.

---

## CATEGORY 0 — NOT gaps (do not "fix")

### Declined destructive gating ladder (user decision 2026-06-14)
Auditors flagged these as HIGH-missing, but they are the intentionally-unbuilt destructive engine group:
- Worker **Drain / Resume / Decommission** + in-flight-task reassign
- Stream **Purge / Backup**, KV bucket **Purge**
- Connection **Drain**, Server **Lame-duck mode**
- The DangerButton + ConfirmModal gating ladder generally

### Honest data-omits (keep — no backing data, do not fabricate)
- Recent operator actions / Audit rows (no audit traffic in seed)
- Trigger fire history; Concurrency "blocked runs" (engine emits no run→gate linkage)
- Connections idle-warn / Consumers backlog callouts (no danger row in seed)
- Services roster (0 registered)

### Enrichments (live exceeds mockup — keep)
- Run list: Range filter + SSE pager; Workflow list: Version/Status cols; Workflow detail: Triggers table; Stream detail page (not in mockup); DLQ/Concurrency summary tiles + type-to-confirm modals.

---

## CATEGORY 1 — Real fidelity gaps to fix (prioritized)

### HIGH — structural / breaks a workflow
1. **Workflow detail: Steps render as raw JSON**, not the mockup's numbered step DAG (dn-step / type pills / `depends_on` edges). [Observe]
2. **Logs: no `Trace ID` column** (and Traces list dropped Trace ID) → kills the log↔trace→span drill. [Trace]
3. **Trace detail: no waterfall/Gantt bars + spans not clickable → no span-detail KV card** (span_id/parent/workflow/step/task/run/status). *(was a known campaign deferral — needs span geometry)* [Trace]
4. **Detail pages missing their stat-tile rows**: Function detail (inv·1h/pending/fail/avg/rate), Worker detail (processed/in-flight/redelivered/dedup) + in-flight-tasks table. [Inventory]

### MED — noticeable structure/columns/controls
5. **Workflow detail: missing `▸ Run workflow` / `Edit definition` header actions + "N steps · DAG" meta.** [Observe]
6. **Run detail: Signal / Cancel buttons missing** from header (only View-trace present) — verify vs campaign claim they were added. [Observe]
7. **Functions list: missing `Invoke` action col, `Pending` col, `service::name` label format.** [Inventory]
8. **Workers list: missing `Group` column** + column reorder vs mockup. [Inventory]
9. **Concurrency sub-tables drop many columns**: slot-pool (Limit/Utilization/Waiting), locks (Mode/Held/Queued/Rejected), rate (Retry-after), debounce (Subject/Window/Absorbed/Fires-in). [Operate]
10. **Services list: columns gutted** — only Service/Registered/Note vs mockup Kind/Version/Commit/Instances/Status/Last-seen + pills + drill-in. [System]
11. **DLQ detail: missing Headers block + stack trace**; confirm full-page detail (not just side-sheet) renders Error+Headers+Payload. [Operate]
12. **Traces/Logs/Audit filter bars reduced**: Traces missing service/since/trace-id inputs+Find; Logs severity is a `<select>` not toggle chips; Audit missing Outcome filter + denied-attempts callout + identity banner. [Trace]
13. **Server: storage % is plain text, not the radial gauge**; JetStream is a flat KV list vs the mockup's stat-card cluster. [System]
14. **Metrics: missing anomaly callout** (amber "snapshot.save anomaly → view runs in window"). [Trace]
15. **Triggers list: inline `Enabled` toggle replaced by a pill; `Fire now` row action missing.** [Observe]
16. **Dashboard: Recent failures is a 2-line block, not the mockup's 3-col `Run id / Workflow / Error` table** with `dn-errsnip` truncation. [Observe]

### LOW — captions / labels / nitpicks
- Workflows list `Avg` (duration) col + "aggregated from WORKFLOW_HISTORY" caption missing.
- Streams: `Policy` column missing; "work" vs "workqueue" pill text.
- Card-head source sublabels missing on several pages (KV-source attributions, "evaluated by trigger-svc", etc.).
- Consumers: lag-`Trend` column dropped (the column frame, separate from spark data).
- Config: Export-YAML disabled btn + effective-config preview table + build-info card missing.
- KV: flat 18-row table vs role-grouped sections + churn column.
- Logs/Traces/Audit source-line wording mismatches (retention copy).
- Functions/Workers/Streams lists carry stat-tiles the mockup lists don't (minor; or mockup puts them in topbar).

---

## Screenshots
`/tmp/audit-obs-*.png`, `/tmp/audit-inv-*.png`, `/tmp/audit-ops-*.png`, `/tmp/audit-sys-*.png`, `/tmp/audit-trc-*.png`
